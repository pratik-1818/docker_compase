version: '3.9'

services:
  fastapi:
    build:
      context: ./app
    container_name: fastapi_app
    ports:
      - "8000:8000"

  streamlit:
    build:
      context: ./streamlit_app
    container_name: streamlit_app
    ports:
      - "8501:8501"
    depends_on:
      - fastapi
